# API Configuration
# This is used for BOTH:
# 1. Frontend API calls (authService, etc.)
# 2. Vite dev server proxy target (development only)
VITE_API_BASE_URL=http://localhost:8080

# Application
VITE_APP_NAME=TikTok Content Publisher

# Environment
VITE_ENVIRONMENT=development

# Allowed Hosts (comma-separated list)
# Examples: localhost,mydomain.com,*.example.com
VITE_ALLOWED_HOSTS=localhost,.localhost
